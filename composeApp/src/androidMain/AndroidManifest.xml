<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Library manifest. Permissions declared here merge into the
         consuming module's manifest at build time. The consuming
         module (currently app/) carries the <application> entry,
         theme, launcher activity, services, receivers, etc.
         Phase 3 of Stage F flips composeApp back to an
         android.application module and absorbs those declarations. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

</manifest>
